namespace Essence
{
    public class Class1
    {
        // Not a "game engine"
        // But a good start to any project

        // Basic structured classes (Vector2Int, Color, Rect, Quaternion, etc.)
        // Simple wrapped classes (Window, Input, Camera)

        // Good enough for most 2D projects that do not need an ECS
        // Usecase: Aether, Resonance and more o.o
    }
}
